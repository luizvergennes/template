{
  "name": "web",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --bun next dev --turbopack",
    "build": "bun --bun next build",
    "start": "bun --bun next start",
    "lint": "biome lint",
    "format": "biome format --write \"**/*.{ts,tsx}\"",
    "typecheck": "tsc --noEmit",
    "release:patch": "bun pm version patch --no-git-tag-version",
    "release:minor": "bun pm version minor --no-git-tag-version",
    "release:major": "bun pm version major --no-git-tag-version",
    "preversion": "docker build -f ./Dockerfile --build-arg APP_VERSION=preversion ../.."
  },
  "dependencies": {
    "@remixicon/react": "^4.9.0",
    "@workspace/db": "workspace:*",
    "@workspace/env": "workspace:*",
    "@workspace/auth": "workspace:*",
    "@workspace/ui": "workspace:*",
    "next": "^16.2.4",
    "next-themes": "^0.4.6",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "@types/react": "^19.2.10",
    "@types/react-dom": "^19.2.3",
    "@workspace/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
